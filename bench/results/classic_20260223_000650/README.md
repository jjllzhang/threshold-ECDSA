# Classic Benchmark Runs

This folder contains raw outputs from `protocol_flow_bench` for several classic threshold settings.
Each `*.txt` file is the full stdout/stderr of one benchmark run (including elapsed wall time).
